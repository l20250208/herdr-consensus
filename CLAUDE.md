# 项目协作规则

本文件适用于所有在 Herdr Consensus 项目中工作的 Coding Agent 和人工维护者。

## 开工前

1. 必须先完整阅读 `DESIGN.md`。
2. 查看 `DESIGN.md` 的“当前阶段”和“实现顺序与进度”，确认现在应执行第几步。
3. 确认前置阶段已经完成，并且本阶段的目标、接口和验收门槛清楚。
4. 只处理当前阶段或用户明确指定的范围，不提前实现后续阶段。

## 工作过程中

1. 方案需要调整时，必须先修改 `DESIGN.md`，说明调整原因、技术影响、接口变化和新的验收标准，然后才能修改代码。
2. 不得先写代码，再补文档合理化已经发生的方案变化。
3. 遵守 `DESIGN.md` 中的数据安全、只读审查、用户裁决、Git worktree 和测试约束。
4. 不覆盖或清理不属于当前任务的用户文件和已有修改。

## 完工后

1. 更新 `DESIGN.md` 中当前阶段的进度状态，写明下一阶段。
2. 在 `CHANGELOG.md` 的 `[Unreleased]` 下追加本次变更说明，必须包含：
   - 修改了哪些文件；
   - 为什么修改；
   - 执行了哪些验证及结果；
   - 下一位维护者需要注意什么。
3. 确认文档描述与实际代码一致，不能把未完成或未验证的内容标为完成。
4. 运行与改动风险相称的测试；测试失败时记录真实原因，不通过删除测试、隐藏错误或建立 baseline 假装成功。
